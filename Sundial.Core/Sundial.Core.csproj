<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <BuildNumber Condition=" '$(APPVEYOR_BUILD_NUMBER)' != '' ">$(APPVEYOR_BUILD_NUMBER)</BuildNumber>
    <!-- for AppVeyor -->
    <BuildNumber Condition=" '$(BUILD_NUMBER)' != '' ">$(BUILD_NUMBER)</BuildNumber>
    <!-- for Team City -->
    <BuildNumber Condition=" '$(BuildNumber)' == '' ">0</BuildNumber>
    <!-- if not set -->
    <TargetFramework>netstandard1.6</TargetFramework>
    <PackageId>sundial.core</PackageId>
    <GeneratePackageOnBuild>True</GeneratePackageOnBuild>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <Product>Sundial Core Library</Product>
    <Authors>James Craig</Authors>
    <Company>James Craig</Company>
    <PackageLicenseUrl>https://github.com/JaCraig/Sundial/blob/master/LICENSE</PackageLicenseUrl>
    <PackageProjectUrl>https://github.com/JaCraig/Sundial</PackageProjectUrl>
    <Description>This is the core library needed to create tasks for Sundial to run. Simply add this package, create a class that inherits from ITimedTask, and you're done coding. Then install Sundial via Chocolatey, open up a command window in the bin directory for this project, and type 'sundial'. The system will then run the tasks and give you a nicely formatted report.</Description>
    <Copyright>Copyright 2017</Copyright>
    <PackageTags>sundial profiler core</PackageTags>
    <NetStandardImplicitPackageVersion>1.6.1</NetStandardImplicitPackageVersion>
    <PackageTargetFallback>$(PackageTargetFallback);dnxcore50</PackageTargetFallback>
    <Version>1.0.$(BuildNumber)</Version>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="BigBook" Version="1.0.18" />
    <PackageReference Include="FileCurator" Version="1.0.10" />
  </ItemGroup>

</Project>